<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>{{ title }}</title>

    <link rel="stylesheet" href="/style.css?v={{ version }}">
</head>

<body>

<h3>INDEX TEMPLATE: {{ site.name }}</h3>

{{! Pass the metalsmith metadata contect to metalsmith-rootpath }}
{{> navigation this }}

{{#each collections.onepager}}
    <section id="{{ id }}">
        <h2>{{ title }} </h2>
        {{{ contents }}}
    </section>
{{/each}}

{{{ contents }}}

{{! Helpers are auto-loaded from the layouts/helpers directory }}
<p>{{ trim '  This should be trimmed via the trim helper. '}}</p>

<script src="/app.bundle.js?v={{ version }}" async></script>

</body>
</html>
